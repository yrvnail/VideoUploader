<mvc:View controllerName="FileUploaderService.controller.App" xmlns:core="sap.ui.core" xmlns:html="http://www.w3.org/1999/xhtml"
	xmlns:mvc="sap.ui.core.mvc" displayBlock="true" xmlns="sap.m">
	<App>
		<Page title="{i18n>fu.app.page.title}" id="appDisplay-MPage" navButtonPress="onNavBack" showNavButton="{device>/system/phone}" showHeader="true" busy="{appView>/busy}"
			busyIndicatorDelay="{appView>/delay}">
			<content>
				<core:Fragment fragmentName="FileUploaderService.fragment.UploadSet" type="XML"/>
			</content>
		</Page>
	</App>
</mvc:View>